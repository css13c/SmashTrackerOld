<UserControl x:Class="SmashTrackerGUI.Views.PlayerView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:SmashTrackerGUI.ViewModels"
			 xmlns:con="clr-namespace:SmashTrackerGUI.Views"
			 mc:Ignorable="d" 
			 d:DesignHeight="300" d:DesignWidth="300">
	<UserControl.Resources>
		<con:CharacterToImageConverter x:Key="CharacterConverter"/>
	</UserControl.Resources>
	<Grid>
		<DockPanel>
			<StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
				<StackPanel.Resources>
					<Style TargetType="Button">
						<Setter Property="Margin" Value="0 0 5 0"/>
					</Style>
				</StackPanel.Resources>
				<Button Content="Add Player" Padding="5,0"/>
				<Button Content="Edit Player" Padding="5,0"/>
				<Button Content="Remove Player" Padding="5,0" Command="{Binding Remove}"/>
				<ComboBox ItemsSource="{Binding SortTypes}" SelectedIndex="{Binding SelectedSort}"/>
			</StackPanel>
			<ListView ItemsSource="{Binding PlayerList}" SelectionMode="Single" SelectedItem="{Binding SelectedPlayer, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" DockPanel.Dock="Top">
				<ListView.ItemTemplate>
					<DataTemplate>
						<StackPanel Margin="10">
							<StackPanel Orientation="Horizontal">
								<Label Content="_Name:"/>
								<TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
							</StackPanel>
							<StackPanel Orientation="Horizontal">
								<Label Content="_Tag:"/>
								<TextBlock Text="{Binding Tag}" VerticalAlignment="Center"/>
							</StackPanel>
							<StackPanel Orientation="Horizontal">
								<Label Content="_Characters:"/>
								<ItemsControl ItemsSource="{Binding Characters}">
									<ItemsControl.ItemsPanel>
										<ItemsPanelTemplate>
											<StackPanel Orientation="Horizontal"/>
										</ItemsPanelTemplate>
									</ItemsControl.ItemsPanel>
									<ItemsControl.ItemTemplate>
										<DataTemplate>
											<Image Source="{Binding Converter={StaticResource CharacterConverter}}"/>
										</DataTemplate>
									</ItemsControl.ItemTemplate>
								</ItemsControl>
							</StackPanel>
							<StackPanel Orientation="Horizontal">
								<Label Content="_Rating:"/>
								<TextBlock Text="{Binding Rating}" VerticalAlignment="Center"/>
							</StackPanel>
						</StackPanel>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
		</DockPanel>
	</Grid>
</UserControl>
